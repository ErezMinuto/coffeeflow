{
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "cd \"${CLAUDE_PROJECT_DIR:-.}\" && git fetch origin 2>&1 || true",
            "statusMessage": "Syncing with team (git fetch)..."
          }
        ]
      }
    ]
  }
}
